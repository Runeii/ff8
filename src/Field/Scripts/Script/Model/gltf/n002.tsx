/* eslint-disable */
// @ts-nocheck

/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 ./models/n002.gltf --types --keepgroups --exportdefault --output=../gltf/n002.tsx 
*/

import * as THREE from 'three'
import React, { useImperativeHandle } from 'react'
import { useGraph } from '@react-three/fiber'
import { useAnimations } from '@react-three/drei';
import { useFragmentedGLTFLoader } from '../useFragmentedGLTFLoader'
import { GLTF, SkeletonUtils } from 'three-stdlib'

type ActionName = 'n002_action_000'

interface GLTFAction extends THREE.AnimationClip {
  name: ActionName
}

type GLTFResult = GLTF & {
  nodes: {
    n002_mesh: THREE.SkinnedMesh
    n002_mesh_1: THREE.SkinnedMesh
    bone_0: THREE.Bone
  }
  materials: {
    n002_texture_0: THREE.MeshStandardMaterial
    n002_texture_1: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

export default React.forwardRef(function n002(props: JSX.IntrinsicElements['group'], ref: React.Ref<{ actions: Record<ActionName, AnimationAction>, mesh: Group }>) {
  const group = React.useRef<THREE.Group>()
  const { scene, animations } = useFragmentedGLTFLoader('n002', props.mapName)
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone) as GLTFResult
  const { actions } = useAnimations(animations, group)

 const formattedAnimations = useAnimations(animations, group);
 useImperativeHandle(ref, () => ({ animations: formattedAnimations, group, nodes, materials }));
   return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="n002_armature" position={[0, 0, 1.7]}>
          <primitive object={nodes.bone_0} />
          <group name="n002">
            <skinnedMesh name="n002_mesh" geometry={nodes.n002_mesh.geometry} material={materials.n002_texture_0} skeleton={nodes.n002_mesh.skeleton} />
            <skinnedMesh name="n002_mesh_1" geometry={nodes.n002_mesh_1.geometry} material={materials.n002_texture_1} skeleton={nodes.n002_mesh_1.skeleton} />
          </group>
        </group>
      </group>
    </group>
 ); })

useFragmentedGLTFLoader.preload('n002')
