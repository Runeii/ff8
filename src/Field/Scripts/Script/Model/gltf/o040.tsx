/* eslint-disable */
// @ts-nocheck

/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 ./models/o040.gltf --types --keepgroups --exportdefault --output=../gltf/o040.tsx 
*/

import * as THREE from 'three'
import React, { useImperativeHandle } from 'react'
import { useGraph } from '@react-three/fiber'
import { useAnimations } from '@react-three/drei';
import { useFragmentedGLTFLoader } from '../useFragmentedGLTFLoader'
import { GLTF, SkeletonUtils } from 'three-stdlib'

type ActionName = 'o040_action_000' | 'o040_action_001'

interface GLTFAction extends THREE.AnimationClip {
  name: ActionName
}

type GLTFResult = GLTF & {
  nodes: {
    o040_mesh: THREE.SkinnedMesh
    o040_mesh_1: THREE.SkinnedMesh
    bone_0: THREE.Bone
  }
  materials: {
    o040_texture_0: THREE.MeshStandardMaterial
    o040_texture_1: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

export default React.forwardRef(function o040(props: JSX.IntrinsicElements['group'], ref: React.Ref<{ actions: Record<ActionName, AnimationAction>, mesh: Group }>) {
  const group = React.useRef<THREE.Group>()
  const { scene, animations } = useFragmentedGLTFLoader('o040', props.mapName)
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone) as GLTFResult
  const { actions } = useAnimations(animations, group)

 const formattedAnimations = useAnimations(animations, group);
 useImperativeHandle(ref, () => ({ animations: formattedAnimations, group, nodes, materials }));
   return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="o040_armature" position={[0, 0, 0.57]}>
          <primitive object={nodes.bone_0} />
          <group name="o040">
            <skinnedMesh name="o040_mesh" geometry={nodes.o040_mesh.geometry} material={materials.o040_texture_0} skeleton={nodes.o040_mesh.skeleton} />
            <skinnedMesh name="o040_mesh_1" geometry={nodes.o040_mesh_1.geometry} material={materials.o040_texture_1} skeleton={nodes.o040_mesh_1.skeleton} />
          </group>
        </group>
      </group>
    </group>
 ); })

useFragmentedGLTFLoader.preload('o040')
