/* eslint-disable */
// @ts-nocheck

/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 ./models/p123.gltf --types --keepgroups --exportdefault --output=../gltf/p123.tsx 
*/

import * as THREE from 'three'
import React, { useImperativeHandle } from 'react'
import { useGraph } from '@react-three/fiber'
import { useAnimations } from '@react-three/drei';
import { useFragmentedGLTFLoader } from '../useFragmentedGLTFLoader'
import { GLTF, SkeletonUtils } from 'three-stdlib'

type ActionName = 'p123_action_0' | 'p123_action_1' | 'p123_action_2' | 'p123_action_3'

interface GLTFAction extends THREE.AnimationClip {
  name: ActionName
}

type GLTFResult = GLTF & {
  nodes: {
    p123_mesh: THREE.SkinnedMesh
    p123_mesh_1: THREE.SkinnedMesh
    bone_0: THREE.Bone
  }
  materials: {
    p123_texture_0: THREE.MeshStandardMaterial
    p123_texture_1: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

export default React.forwardRef(function p123(props: JSX.IntrinsicElements['group'], ref: React.Ref<{ actions: Record<ActionName, AnimationAction>, mesh: Group }>) {
  const group = React.useRef<THREE.Group>()
  const { scene, animations } = useFragmentedGLTFLoader('p123', props.mapName)
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone) as GLTFResult
  const { actions } = useAnimations(animations, group)

 const formattedAnimations = useAnimations(animations, group);
 useImperativeHandle(ref, () => ({ animations: formattedAnimations, group, nodes, materials }));
   return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="p123_armature" position={[0, 0, 1.618]}>
          <primitive object={nodes.bone_0} />
          <group name="p123">
            <skinnedMesh name="p123_mesh" geometry={nodes.p123_mesh.geometry} material={materials.p123_texture_0} skeleton={nodes.p123_mesh.skeleton} />
            <skinnedMesh name="p123_mesh_1" geometry={nodes.p123_mesh_1.geometry} material={materials.p123_texture_1} skeleton={nodes.p123_mesh_1.skeleton} />
          </group>
        </group>
      </group>
    </group>
 ); })

useFragmentedGLTFLoader.preload('p123')
