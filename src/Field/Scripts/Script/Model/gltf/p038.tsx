/* eslint-disable */
// @ts-nocheck

/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 ./optimized/base/models/p038.gltf --types --keepgroups --exportdefault --output=./gltf/p038.tsx 
*/

import * as THREE from 'three'
import React, { useImperativeHandle } from 'react'
import { useGraph } from '@react-three/fiber'
import { useAnimations } from '@react-three/drei';
import { useFragmentedGLTFLoader } from '../useFragmentedGLTFLoader'
import { GLTF, SkeletonUtils } from 'three-stdlib'

type ActionName = 'p038_action_000' | 'p038_action_001' | 'p038_action_002' | 'p038_action_003'

interface GLTFAction extends THREE.AnimationClip {
  name: ActionName
}

type GLTFResult = GLTF & {
  nodes: {
    p038_mesh_0: THREE.SkinnedMesh
    bone_0: THREE.Bone
  }
  materials: {
    p038_mesh_0_texture_0: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

export default React.forwardRef(function p038(props: JSX.IntrinsicElements['group'], ref: React.Ref<{ actions: Record<ActionName, AnimationAction>, mesh: Group }>) {
  const group = React.useRef<THREE.Group>()
  const { scene, animations } = useFragmentedGLTFLoader('p038', props.mapName)
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone) as GLTFResult
  const { actions } = useAnimations(animations, group)

 const formattedAnimations = useAnimations(animations, group);
 useImperativeHandle(ref, () => ({ animations: formattedAnimations, group, nodes, materials }));
   return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="p038_armature">
          <primitive object={nodes.bone_0} />
        </group>
        <skinnedMesh name="p038_mesh_0" geometry={nodes.p038_mesh_0.geometry} material={materials.p038_mesh_0_texture_0} skeleton={nodes.p038_mesh_0.skeleton} />
      </group>
    </group>
 ); })

useFragmentedGLTFLoader.preload('p038')
