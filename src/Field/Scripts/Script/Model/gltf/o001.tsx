/* eslint-disable */
// @ts-nocheck

/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 ./models/o001.glb --types --exportdefault --output=./gltf/o001.tsx 
*/

import * as THREE from 'three'
import React, { useImperativeHandle } from 'react'
import { useGraph } from '@react-three/fiber'
import { useGLTF, useAnimations } from '@react-three/drei'
import { GLTF, SkeletonUtils } from 'three-stdlib'

type ActionName = 'o001_action_0' | 'o001_action_1' | 'o001_action_2' | 'o001_action_3' | 'o001_action_4' | 'o001_action_5' | 'o001_action_6' | 'o001_action_7'

interface GLTFAction extends THREE.AnimationClip {
  name: ActionName
}

type GLTFResult = GLTF & {
  nodes: {
    o001: THREE.SkinnedMesh
    bone_1: THREE.Bone
  }
  materials: {
    o001: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

export default React.forwardRef(function o001(props: JSX.IntrinsicElements['group'], ref: React.Ref<{ actions: Record<ActionName, AnimationAction>, mesh: Group }>) {
  const group = React.useRef<THREE.Group>()
  const { scene, animations } = useGLTF('/models/o001.glb')
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone) as GLTFResult
  const { actions } = useAnimations(animations, group)
    
    const formattedAnimations = useAnimations(animations, group);
    useImperativeHandle(ref, () => ({ animations: formattedAnimations, group, nodes, materials }));
      return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="o001_armature">
          <primitive object={nodes.bone_1} />
          <skinnedMesh name="o001" geometry={nodes.o001.geometry} material={materials.o001} skeleton={nodes.o001.skeleton} />
        </group>
      </group>
    </group>
    ); })

useGLTF.preload('/models/o001.glb')
